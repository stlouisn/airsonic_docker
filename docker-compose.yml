version: '2.3'

services:

  airsonic:
    image: stlouisn/airsonic:latest
    container_name: airsonic
    hostname: airsonic
    environment:
      - TZ=UTC
    restart: always
    volumes:
      - ./airsonic/config:/config:rw
      - ./airsonic/music:/music:rw
      - ./airsonic/playlists:/playlists:rw
      - ./airsonic/podcasts:/podcasts:rw
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - 4040
